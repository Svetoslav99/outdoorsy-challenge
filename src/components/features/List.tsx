import classes from './List.module.css';

const List: React.FC = (props) => {

    return (
        <>
            {props.list.map((item) => (
        // TODO: create your cards here 

            ))}
        </>
    );
};

export default List;
